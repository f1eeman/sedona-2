# Синтаксис .gitignore. Пути без ведущего "./" — иначе шаблон не сработает.

# Зависимости и сборка
node_modules/
dist/
build/
.astro/
coverage/

# Статика: отдаём как есть, форматирование может её сломать
public/

# Служебные каталоги
.vscode/
.idea/
.claude/
.husky/
.yarn/

# Рабочие документы агентов: спеки и планы держат намеренно частичные
# фрагменты кода (тело interface без файла, одно свойство конфига), а prettier
# форматирует код внутри markdown-блоков и падает на них SyntaxError
docs/superpowers/

# Логи и локальные файлы
*.log
*.local
